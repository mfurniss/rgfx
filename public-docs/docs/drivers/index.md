# Drivers

ESP32 microcontrollers run the RGFX driver firmware and control connected LED hardware.

Each driver uses a single data pin for simplicity. A single ESP32 can typically drive up to 1,000 LEDs at 5V on one data line.
