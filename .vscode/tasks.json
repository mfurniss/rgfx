{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Upload via Serial (USB)",
      "type": "shell",
      "command": "${workspaceFolder:esp32}/upload-all-serial.sh",
      "problemMatcher": ["$platformio"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    },
    {
      "label": "Upload via OTA (WiFi)",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "if [ \"${input:otaDevice}\" = \"all\" ]; then ${workspaceFolder:esp32}/upload-all-ota.sh; else cd ${workspaceFolder:esp32} && pio run -t upload --upload-port ${input:otaDevice}; fi"
      ],
      "problemMatcher": ["$platformio"],
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    },
    {
      "label": "ESP32: Test (Native)",
      "type": "shell",
      "command": "pio",
      "args": [
        "test",
        "-e",
        "native",
        "--project-dir",
        "${workspaceFolder:esp32}"
      ],
      "problemMatcher": ["$platformio"],
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    }
  ],
  "inputs": [
    {
      "id": "otaDevice",
      "type": "pickString",
      "description": "Select device for OTA upload",
      "options": [
        {
          "label": "All Devices",
          "value": "all"
        }
      ],
      "default": "all"
    }
  ]
}
